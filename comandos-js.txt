// Tamanho da String
variavel.length

// Transformar as letras em Maiúsculas
variavel.toUpperCase()

// Transformar as letras para Minusculas
variavel.toLowerCase()

// Transformar o numero para inteiro e numero real
Number.parseInt(numero)
Number.parseFloat(numero)

// Mensagem ao usuario 
window.alert('Mensagem')

// Coletando dados do usuario
window.prompt('Digite seu nome ')

// Enviar pergunta com resposta 
window.confirm('Gostou do curso ?')

// Definindo variavel
var n1 = 5

// Escrevendo na tela 
document.write(`Seu nome tem ${nome.length} letras`)

// Definindo a quantidade de casas decimais
numero.toFixed(2)

// Trocando caracteres
string.replace('.',';')

// Formatando o número em moeda
numero.toLocaleString('pt-BR', {style: 'currency',currency:'BRL'})

// Incrementando a varialvel
varialvel += 1
variavel -= 1
variavel *= 1
variavel /= 1
variavel **= 2
variavel %= 5

// Incrementando 1 a variavel
variavel ++
variavel --

// Operador de identidade
5 === '5' // Verificando se são do mesmo valor e tipo.

// Operadores lógicos
!  // NOT 
&& // AND
|| // OR

// Operador Ternario , igual ao if
teste  ?  true  :  false
media >= 7  ?   "Aprovado"  :  "Reprovado"
var numero  1<10 ? 5 : 10

// Estrutura condicional if
if (condicão){

}else{

}

// Escrevendo mensagem no console do node js
console.log('Hello World !!!')

// Comando switch
switch (expressão){
    cas1 valor1:

    break

    case2 valor2:

    break

    default:

    break

}

// Comando while
while (condição){
    execute
}

// Comando do
do {

} while (condição)

// Comando for 
for (inicio ; teste ; incremento){
    
}

// Acrescentando valores a uma array
array.push(valor)

// Comprimento de uma array
array.length

// Ordenando os valores em ordem crescente em uma array
array.sort()

// Percorrendo uma array
for (let posicao in vetor){
    console.log(vetor[posicao])
}

// Buscando o index de um vetor
vetor.indexOf(valor)

// Declarando uma função com parametros
function acao(param){

    return res
}

acao(5)

// Declarando um dicionário
let amigo = {nome:'josé, sexo:'masculino', idade:56}